// Huffman.cpp — Stub implementation (Step 2)
#include "algo/Huffman.hpp"
#include <stdexcept>

namespace algo {

std::vector<uint8_t> Huffman::compress(const uint8_t* /*data*/, size_t /*size*/) {
    throw std::runtime_error("Huffman::compress not yet implemented (Step 2)");
}

std::vector<uint8_t> Huffman::decompress(const uint8_t* /*data*/, size_t /*size*/) {
    throw std::runtime_error("Huffman::decompress not yet implemented (Step 2)");
}

} // namespace algo
